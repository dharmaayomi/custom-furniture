import useAxios from "@/hooks/useAxios";

const useGetDeliveryFeeEstimates = (
  addressId?: number,
  configuration?: any,
) => {
  const axiosInstance = useAxios();
  return;
  return useGetDeliveryFeeEstimates;
};

export default useGetDeliveryFeeEstimates;
