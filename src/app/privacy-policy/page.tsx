import { PuffLoader } from "@/components/ui/puff-loader";
import React from "react";

const Privacy = () => {
  return (
    <div>
      <PuffLoader size={80} color="var(--color-primary)" speed={1.7} />
    </div>
  );
};

export default Privacy;
